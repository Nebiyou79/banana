# =========================
# BUILD STAGE
# =========================
FROM node:18-alpine AS builder

WORKDIR /app

# Install dependencies first (better caching)
COPY package.json package-lock.json* ./
RUN npm ci

# Copy source
COPY . .

# Ensure Next.js standalone output is enabled
# (FAIL build if missing instead of silently creating files)
RUN node -e "const c=require('./next.config.js'); if(!c.output || c.output!=='standalone'){console.error('❌ next.config.js must use output: standalone'); process.exit(1)}"

# Build Next.js
RUN npm run build


# =========================
# PRODUCTION STAGE
# =========================
FROM node:18-alpine AS runner

WORKDIR /app

ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1
ENV PORT=3000
ENV HOSTNAME=0.0.0.0

# Create non-root user
RUN addgroup -g 1001 -S nodejs \
 && adduser -S nextjs -u 1001

# Copy standalone output
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
COPY --from=builder --chown=nextjs:nodejs /app/public ./public

USER nextjs

EXPOSE 3000

# Standalone entry (generated by Next.js)
CMD ["node", "server.js"]
